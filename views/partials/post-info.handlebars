<div class="card">
	<div class="card-header">
		<h4 class="card-title">
		<a href="/post/{{id}}">{{title}}</a></h4>
		<h6 class="card-subtitle">Posted by {{user.username}} on {{format_date created_at}}</h6>
	</div>
	<div class="card-body">
		<p class="card-text">{{content}}</p>
	</div>
</div>